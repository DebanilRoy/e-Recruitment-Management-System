The database was built on MySQL v8.0.36

Some of the crucial MySQL procedures are in seperate files for reference.

The complete database schema can be found in the SQL dump file included. The dump can also be used to import the schema into a MySQL instance.